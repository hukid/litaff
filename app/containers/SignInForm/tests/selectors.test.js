// import { fromJS } from 'immutable';
// import { makeSelectSignInFormDomain } from '../selectors';

// const selector = makeSelectSignInFormDomain();

describe('makeSelectSignInFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
