// import { fromJS } from 'immutable';
// import { makeSelectSharingAgendaDomain } from '../selectors';

// const selector = makeSelectSharingAgendaDomain();

describe('makeSelectSharingAgendaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
