// import { fromJS } from 'immutable';
// import { makeSelectSchedulePageDomain } from '../selectors';

// const selector = makeSelectSchedulePageDomain();

describe('makeSelectSchedulePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
