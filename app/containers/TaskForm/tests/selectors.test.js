// import { fromJS } from 'immutable';
// import { makeSelectTaskFormDomain } from '../selectors';

// const selector = makeSelectTaskFormDomain();

describe('makeSelectTaskFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
