// import { fromJS } from 'immutable';
// import { makeSelectCoworkerPageDomain } from '../selectors';

// const selector = makeSelectCoworkerPageDomain();

describe('makeSelectCoworkerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
