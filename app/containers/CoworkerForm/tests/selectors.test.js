// import { fromJS } from 'immutable';
// import { makeSelectCoworkerFormDomain } from '../selectors';

// const selector = makeSelectCoworkerFormDomain();

describe('makeSelectCoworkerFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
