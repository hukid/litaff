// import { fromJS } from 'immutable';
// import { makeSelectSignUpFormDomain } from '../selectors';

// const selector = makeSelectSignUpFormDomain();

describe('makeSelectSignUpFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
